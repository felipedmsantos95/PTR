#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "sistema.h"
#include "simulacao.h"


int main(int argc, char *argv[])
{
    
    
    //Área de teste
    /*
    double v = 30;
    double w = 45;

    double * x = (double *) calloc (3, sizeof (double));
     x = yt(v, w);
    printf("%lf %lf %lf\n", x[0], x[1], x[2]);
    free(x);*/

    simulacao();

    return 0;
}
